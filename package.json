{
  "name": "worker-typescript-template",
  "version": "1.0.0",
  "private": true,
  "description": "Testing MongoDB Realm w/ Cloudflare Workers",
  "scripts": {
    "build": "worktop build src/index.ts",
    "types": "tsc --noEmit --skipLibCheck"
  },
  "license": "MIT OR Apache-2.0",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.1.1",
    "typescript": "4.4.3",
    "worktop.build": "0.0.3"
  },
  "dependencies": {
    "realm-web": "^1.5.1",
    "sunder": "^0.9.4"
  }
}
